# Alles hier laeuft am Ende unter Linux. Ohne LF-Zwang kippt run.sh auf einem
# Windows-Rechner auf CRLF und der Container stirbt mit
# "/bin/sh^M: bad interpreter: No such file or directory".
* text=auto eol=lf
